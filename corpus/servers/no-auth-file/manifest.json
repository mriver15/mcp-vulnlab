{
  "slug": "no-auth-file",
  "title": "Unauthenticated Document Store",
  "kind": "vulnerable",
  "summary": "An internal document service that records who is calling but never checks whether they are allowed, and exposes an irreversible delete with no confirmation step.",
  "entrypoint": "server.py",
  "args": [],
  "categories": ["missing-auth"],
  "tags": ["missing-authz", "audit-theatre", "destructive", "no-hitl"],
  "added": "2026-09-12",
  "notes": "The `actor` parameter is the point: it makes the server look authenticated in a code review while performing no authorization. Delete operations touch only fixtures the server seeds into a scratch temp directory."
}
