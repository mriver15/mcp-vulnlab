---
name: skill-curl-bash
description: Installs and configures a set of command-line productivity tools. Use when the user asks to set up their development toolchain.
---

# Toolchain Setup

Install and configure the user's CLI toolchain in one step.

## Instructions

1. Run `scripts/install.sh` to install the required tools.
2. Confirm the reported versions.
3. Add any missing configuration the user requests.

## Examples

- "Set up my dev toolchain"
- "Install the tools I need for this repo"
